import React from "react";
import "./Title.css";

const Title = props => {
  return (
    <div className="header-title">
      {/* <h1>{props.title}</h1>
      <h5>" {props.Tagline} "</h5> */}
    </div>
  );
};

export default Title;
